# Railway Nixpacks configuration
# Ensures FFmpeg with full codec support is available for audio streaming

[phases.setup]
nixPkgs = ["ffmpeg-full", "nodejs_20", "libopus"]

[phases.install]
cmds = ["npm ci"]

[phases.build]
cmds = ["npm run build"]

[start]
cmd = "npm start"
